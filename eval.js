const ids = {
	lynxIR: 'jsPrim'
}
let memoTable = {}
const getValue = (id, attrId) => {
	const key = id+attrId //refer to values by their key
	if memoTable.hasOwnProperty(key){
		return memoTable[key]
	}
	if (attrId === ids.lynxIR){
		return
	}

}

const primitives:{

}
string.characterarray = app.keyboard.ispressed ?
	concat(string.prevVal && app.keyboard.keycode)
getValue('app', 'childElements')
